<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activityLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:onClick="onCloseThis"
    android:background="@android:color/transparent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:gravity="bottom">
        <GridView
            android:id="@+id/change_send_gridview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@+id/close_layout"
            android:scrollbars="none"
            android:numColumns="4"
            android:layout_marginLeft="@dimen/dp_12.5"
            android:layout_marginRight="@dimen/dp_12.5"
            android:layout_marginBottom="@dimen/dp_57"
            />

        <RelativeLayout
            android:id="@+id/close_layout"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_49"
            android:layout_alignParentBottom="true"
            android:background="#fefefe"
            android:clickable="true"
            android:gravity="center"
            android:onClick="onCloseThis">
            <ImageView
                android:id="@+id/close_image"
                android:layout_width="@dimen/dp_22"
                android:layout_height="@dimen/dp_22"
                android:layout_centerInParent="true"
                android:scaleType="centerInside"
                android:src="@drawable/i_changesend"
                android:visibility="visible"/>
        </RelativeLayout>
    </RelativeLayout>

</RelativeLayout>